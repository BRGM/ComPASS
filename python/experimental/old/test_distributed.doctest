
>>> #from mpi4py import MPI
>>> #comm = MPI.COMM_WORLD
>>> #rank = comm.Get_rank()
>>> #comm.gather(rank)


>>> from distributed import ZoneManager

>>> manager = ZoneManager(10)
>>> #manager.distribute([])
